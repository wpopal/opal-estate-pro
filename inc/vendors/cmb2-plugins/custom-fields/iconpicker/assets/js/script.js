( function ( $ ) {
    'use strict';
    $( document ).ready( function () {
        $( '.opalestate-iconpicker' ).each( function () {
            $( this ).fontIconPicker();
        } );
    } );

} )( jQuery );
